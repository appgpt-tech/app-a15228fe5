//Source code generated by AppGPT (www.appgpt.tech)
import {
  Datagrid,
  List,
  EditButton,
  Edit,
  SimpleForm,
  Create,
  SelectColumnsButton,
  DatagridConfigurable,
  TopToolbar,
  CreateButton,
  ExportButton,
  FilterButton,
  //Field controls
  BooleanField,
  DateField,
  EmailField,
  ImageField,
  NumberField,
  ReferenceField,
  TextField,
  UrlField,
  //Input controls
  BooleanInput,
  DateInput,
  EmailInput,
  ImageInput,
  NumberInput,
  ReferenceInput,
  TextInput,
  UrlInput,
  PasswordInput,
} from 'react-admin';
import { useRecordContext } from 'react-admin';
import { Grid } from '@mui/material';
const ReadOnlyPasswordField = ({ record, source }) => {
  // You can customize the way you display the password here, e.g., mask it with asterisks
  const maskedPassword = '********';

  return <span>{maskedPassword}</span>;
};
const ListActions = () => (
  <TopToolbar>
    <FilterButton />
    <CreateButton />
    <ExportButton />
    <SelectColumnsButton />
  </TopToolbar>
);
const LeaderboardsTitle = () => {
  const record = useRecordContext();
  return <span>Leaderboards {record ? `"${record.playerName}"` : ''}</span>;
};

export const LeaderboardsList = () => (
  <List actions={<ListActions />} filters={ResourceFilters}>
    <DatagridConfigurable>
      <ReferenceField source="playerName" reference="Players" />
      <NumberField source="playerScore" />
      <NumberField source="rank" />
      <EditButton />
    </DatagridConfigurable>
  </List>
);

export const LeaderboardsEdit = () => (
  <Edit title={<LeaderboardsTitle />}>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <ReferenceInput source="playerName" reference="Players" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="playerScore" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="rank" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Edit>
);

export const LeaderboardsCreate = () => (
  <Create>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <ReferenceInput source="playerName" reference="Players" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="playerScore" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="rank" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Create>
);

const ResourceFilters = [
  <TextInput source="q" label="Search" alwaysOn />,
  <ReferenceInput
    source="playerName"
    label="playerName"
    reference="Players"
    alwaysOn
  />,
  ,
  ,
];
